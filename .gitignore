# 1. تجاهل كل شيء في المجلد الرئيسي افتراضياً
/*

# 2. استثناء المجلدات التي تريد رفعها فقط
!src/
!GLM-front/
!.gitignore

# 3. داخل مجلد src: تجاهل مجلد test
src/test/

# 4. داخل مجلد GLM-front: استثناء الملفات الضرورية فقط
# (بما أننا تجاهلنا كل شيء في البداية، نحتاج لاستثناء محتويات الفولدر)
!GLM-front/**

# 5. تجاهل ملفات النظام والمكتبات داخل GLM-front (اختياري ولكن مهم)
GLM-front/node_modules/
GLM-front/.next/
GLM-front/out/
GLM-front/build/